# Security and Safety

Do not expose the Kinect controller control port to an untrusted network. If remote control is needed, set `CONTROL_API_TOKEN` and restrict network access.

Robot safety is physical safety. Test with dry-run modes first, calibrate carefully, and keep people clear of the robot workspace.
